{
  "name": "TEMPLATE_PROJECT_NAME",
  "version": "1.0.0",
  "description": "TEMPLATE_PROJECT_NAME Webflow site — JS bundle served via jsDelivr",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
